specVersion: "v1beta"
serviceId: "GP_AI"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "GP_AI-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
